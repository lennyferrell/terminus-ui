import { TsChipComponent } from './chip.component';


describe(`TsChipComponent`, () => {
  let component: TsChipComponent;

  beforeEach(() => {
    component = new TsChipComponent();
  });


  test(`should exist`, () => {
    expect(component).toBeTruthy();
  });

});
